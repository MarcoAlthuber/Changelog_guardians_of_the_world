name: Oznámenie releasu na Discord

on:
  release:
    types: [published]

jobs:
  oznam-na-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Poslať správu na Discord
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK }}
          TAG_NAME: ${{ github.event.release.tag_name }}
          RELEASE_NAME: ${{ github.event.release.name }}
          RELEASE_BODY: ${{ github.event.release.body }}
          RELEASE_TIME: ${{ github.event.release.published_at }}
        run: |
          ESCAPED_BODY=$(printf "%s" "$RELEASE_BODY" | python3 -c "import json,sys; print(json.dumps(sys.stdin.read()))")
          
          JSON=$(cat <<EOF
{
  "embeds": [{
    "title": "🚀 Nová verzia bola vydaná: $TAG_NAME",
    "description": "**📌 Názov:** $RELEASE_NAME\n\n📝 **Zoznam zmien:**\n${ESCAPED_BODY}",
    "color": 16753920,
    "timestamp": "$RELEA
